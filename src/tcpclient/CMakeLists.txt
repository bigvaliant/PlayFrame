MESSAGE(STATUS "CMAKE_CURRENT_SOURCE_DIR" ${CMAKE_CURRENT_SOURCE_DIR})
#设置编译好程序存放路径
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/../run/tcpclient/bin)
AUX_SOURCE_DIRECTORY(. SRC)
ADD_EXECUTABLE(tcpclient ${SRC})
TARGET_LINK_LIBRARIES(tcpclient ${STATIC_LIBS} ${DYN_LIBS})

