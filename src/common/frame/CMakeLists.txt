MESSAGE(STATUS "CMAKE_CURRENT_SOURCE_DIR " ${CMAKE_CURRENT_SOURCE_DIR})
#设置编译好的程序存放路径
SET(LIBRARY_OUTPUT_PATH ${CMAKE_CURRENT_SOURCE_DIR})
AUX_SOURCE_DIRECTORY(. FRAME_SRC)
ADD_LIBRARY(gameframe STATIC ${FRAME_SRC})
